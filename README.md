# eslint-config-farcast

Install package
```
npm install ssh+git://git@github.com:limianwang/eslint-config-farcast.git --save-dev
```

Create `.eslintrc.js`

```
module.exports = {
	"extends": "farcast"
};

```

